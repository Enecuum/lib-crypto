var addon = require('bindings')('addon');


	var a = addon.BigNumber(25);
	var b = addon.BigNumber(978);
	var p = addon.BigNumber(1223);
	var order = addon.BigNumber(1183);
	var g0x = addon.BigNumber(972);
	var g0y = addon.BigNumber(795);

	// var curve = addon.createCurve(a, b, p, order, g0x, g0y);

	// var msk = addon.BigNumber(10);// = getRandom(BigNumber(1223));

	// var q = addon.BigNumber(13);	// G0 order
	// var gx = addon.BigNumber(1158);
	// var gy = addon.BigNumber(92);


	// var G0 = curve.createPoint(g0x, g0y)
	// var G = curve.createPoint(gx, gy)

	// var MPK = createMPK(msk, G0, curve1);


	// std::cout << "\r\n      PKG keys generation \r\n";

	// int KblockID = 123;
	// int LPoSID = 677321;
	// // r следует брать соучайно r = ZZ.random_element(q)
	// var r = getRandom(q);

	// var Q = mul(r, G, curve1);

	